diff --git a/conda/common/_os/osx.py b/conda/common/_os/osx.py
index 971be9b54..638a79eb5 100644
--- a/conda/common/_os/osx.py
+++ b/conda/common/_os/osx.py
@@ -1,7 +1,7 @@
 # Copyright (C) 2012 Anaconda, Inc
 # SPDX-License-Identifier: BSD-3-Clause
 import platform
-from subprocess import check_output
+import sys
 
 
 def mac_ver() -> str:
@@ -11,8 +11,12 @@ def mac_ver() -> str:
     If Python was compiled against macOS <=10.15, we might get 10.16 instead of 11.0.
     For these cases, we must set SYSTEM_VERSION_COMPAT=0 and call sw_vers directly.
     """
+    if sys.platform == "emscripten":
+        return ""
     distribution_version = platform.mac_ver()[0]
     if distribution_version == "10.16":
+        from subprocess import check_output
+
         return check_output(
             ["/usr/bin/sw_vers", "-productVersion"],
             env={"SYSTEM_VERSION_COMPAT": "0"},
diff --git a/conda/gateways/subprocess.py b/conda/gateways/subprocess.py
index 830090301..36869f5e7 100644
--- a/conda/gateways/subprocess.py
+++ b/conda/gateways/subprocess.py
@@ -43,7 +43,16 @@ def _format_output(command_str, cwd, rc, stdout, stderr):
     ) % (command_str, cwd, rc, stdout, stderr)
 
 
+def _check_subprocess_available():
+    import sys
+    if sys.platform == "emscripten":
+        raise RuntimeError(
+            "subprocess is not available in the browser environment"
+        )
+
+
 def any_subprocess(args: Sequence[str], prefix, env=None, cwd=None):
+    _check_subprocess_available()
     script_caller, command_args = wrap_subprocess_call(
         context.root_prefix,
         prefix,
@@ -85,6 +94,7 @@ def subprocess_call(
     """This utility function should be preferred for all conda subprocessing.
     It handles multiple tricky details.
     """
+    _check_subprocess_available()
     env = encode_environment(env or os.environ)
     cwd = sys.prefix if path is None else abspath(path)
     if not isiterable(command):
